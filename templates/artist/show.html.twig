{% extends 'base.html.twig' %}



{% block title %}ConcertArtist{% endblock %}

{% block body %}




    <h1>ConcertArtist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ concert_artist.id }}</td>
            </tr>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ concert_artist.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ concert_artist.description }}</td>
            </tr>
            <tr>
                <th>ImgName</th>
                <td>{{ concert_artist.imgName }}</td>
                <td><img style="width: 180px; height: 240px;" src="{{ asset('img/artists/' ~ concert_artist.imgName) }}" class="img-fluid rounded-start" alt="..."></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artist_index') }}">back to list</a>

    <a href="{{ path('artist_edit', {'id': concert_artist.id}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
