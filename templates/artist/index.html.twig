{% extends 'base.html.twig' %}

{% block title %}ConcertArtist index{% endblock %}

{% block body %}
    <h1>ConcertArtist index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>ImgName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for concert_artist in concert_artists %}
            <tr>
                <td>{{ concert_artist.id }}</td>
                <td>{{ concert_artist.name }}</td>
                <td>{{ concert_artist.description }}</td>
                <td>{{ concert_artist.imgName }}</td>
                <td>
                    <a href="{{ path('artist_show', {'id': concert_artist.id}) }}">show</a>
                    <a href="{{ path('artist_edit', {'id': concert_artist.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('artist_new') }}">Create new</a>
{% endblock %}
